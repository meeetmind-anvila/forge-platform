name: conflict-demo
version: 0.1.0
dependencies:
  - name: lib-core
    version: ">=1.0.0 <2.0.0"
  - name: lib-core
    version: ">=2.0.0 <3.0.0"
jobs:
  build:
    runtime: alpine:3.18
    steps:
      - { name: should-not-run, run: "echo this must not run" }
